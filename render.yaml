services:
  - type: web
    name: postgresql-dashboard
    env: node
    buildCommand: |
      npm install
      npm run build
      cd backend
      npm install
    startCommand: node backend/server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
